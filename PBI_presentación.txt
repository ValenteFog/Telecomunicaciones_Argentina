Medidas:

Promedio_ingresos = AVERAGE(tabla_ingresos[monto_ingresos])
Total_ingresos = SUM(tabla[columna])
Total_de_algo = COUNT(tabla[id])


Gráficos de líneas:
Evolución de cantidades
Evolución de precio

Tablas:
Precio promedio por tipo de operación
Precio promedio por tipo de cosa


Diagrama PBI:
Portada
Introducción
Productos?


Tarjetas:
ingresos histórico total (en millones)
el más algo
años (slider)

Gráfico de barras:
top 5 mayor penetración
top 5 menor penetración

Medidas:
Acceso a Internet % = DIVIDE(SUM(Internet_localidad[Total general]), CALCULATE(SUM(Internet_localidad[Total general]), ALL(Internet_localidad)))

Variación Accesos 100 SUM Trim Previo % = 
VAR Accesos100SUMTP =
CALCULATE([Accesos 100 SUM], DATEADD(Calendario[Date], -1, QUARTER))
RETURN
IF(MAX(Calendario[Date]) < DATE(2022, 4, 1), DIVIDE(([Accesos 100 SUM]-Accesos100SUMTP), Accesos100SUMTP), BLANK())

Telefonía Fija Total SUM = SUM(telefonia_fija_localidad[Accesos telefonía fija (total)])

Accesos Telefonía Fija 100 SUM Trim Previo = CALCULATE([Acceso Telefonía Fija 100 SUM],DATEADD(Calendario[Date],-1,QUARTER))

Accesos 100 SUM Trim Previo = CALCULATE([Accesos 100 SUM],DATEADD(Calendario[Date],-1,QUARTER))

Accesos 100 SUM = SUM(Accesos100[Accesos por cada 100 hogares])

Acceso a Internet % = 
DIVIDE(SUM(Internet_localidad[Total general]), CALCULATE(SUM(Internet_localidad[Total general]), ALL(Internet_localidad)))
